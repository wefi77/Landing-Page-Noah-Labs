/* Community growth: new on-chain blocks keep joining the constellation. */
const map=document.querySelector('.community-map');
if(map){let count=0;const addBlock=()=>{if(count>16)return;const block=document.createElement('span');block.className='growth-block';const angle=Math.random()*Math.PI*2,radius=105+Math.random()*130;block.style.left=`calc(50% + ${Math.cos(angle)*radius}px)`;block.style.top=`calc(50% + ${Math.sin(angle)*radius}px)`;block.style.setProperty('--x',`${-Math.cos(angle)*42}px`);block.style.setProperty('--y',`${-Math.sin(angle)*42}px`);map.append(block);count++};for(let i=0;i<7;i++)setTimeout(addBlock,i*260);setInterval(addBlock,1700)}
const button=document.querySelector('.learn-button'),menu=document.querySelector('.contact-menu');if(button&&menu){button.addEventListener('click',()=>{const open=menu.classList.toggle('open');button.setAttribute('aria-expanded',open);menu.setAttribute('aria-hidden',!open)});document.addEventListener('click',e=>{if(!e.target.closest('.actions')){menu.classList.remove('open');button.setAttribute('aria-expanded','false');menu.setAttribute('aria-hidden','true')}})}
