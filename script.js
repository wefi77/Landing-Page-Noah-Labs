const canvas=document.getElementById('stars'),ctx=canvas.getContext('2d');let w,h,stars=[];function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;stars=Array.from({length:Math.min(180,Math.floor(w*h/8500))},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*1.35+.18,a:Math.random(),s:Math.random()*.12+.015}))}function draw(){ctx.clearRect(0,0,w,h);stars.forEach((p,i)=>{p.y-=p.s;if(p.y<0)p.y=h;let a=.25+Math.sin(Date.now()/1200+i)*.2;ctx.fillStyle=`rgba(190,230,255,${a})`;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,7);ctx.fill()});requestAnimationFrame(draw)}resize();draw();addEventListener('resize',resize);
const counters=document.querySelectorAll('[data-count]');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting||e.target.dataset.done)return;const el=e.target,end=Number(el.dataset.count),suffix=el.dataset.suffix||'',start=performance.now(),duration=1500;function tick(t){const v=Math.min((t-start)/duration,1),n=end*(1-Math.pow(1-v,3));el.textContent=(end%1?n.toFixed(2):Math.floor(n).toLocaleString())+suffix;if(v<1)requestAnimationFrame(tick)}requestAnimationFrame(tick);el.dataset.done=1}),{threshold:.5});counters.forEach(c=>observer.observe(c));
